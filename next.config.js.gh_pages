/** @type {import('next').NextConfig} */
const nextConfig = {
	output: 'export',
	distDir: 'dist',
	basePath: '/incident-commander',
	images: {
		unoptimized: true,
	},
}

module.exports = nextConfig
